> 弃用说明（2026-08-26）：AI 情绪分析功能当前阶段不完善，6 个 AI 因子
> （ai_sentiment_* / ai_attention_* / ai_topic_momentum）已从策略引擎移除，
> 本文档仅保留作为历史设计参考。AI 舆情分析（新闻采集 / 情绪聚合 / 市场研判）
> 仍作为独立展示功能保留。

## 目标

我希望从以下GitHub项目中提取核心能力，并集成到我的指数量化系统中：

项目地址： https://github.com/sansan0/TrendRadar.git

我已经将项目拉取到TrendRadar目录下

但要求不是简单复制代码，而是：

**抽象核心能力 + 解耦 + 适配量化系统接口 + 形成独立模块**

我希望给系统新增一层：**AI舆情 / 新闻 / 热点因子层**

------

## 你的任务

请完成以下工作：

------

### 1️⃣ 项目分析（必须）

分析 TrendRadar 项目，拆解出：

- 核心功能模块
- 数据来源模块
- AI分析模块
- 热点/趋势计算模块
- 不必要的UI/服务模块（必须剔除）

输出结构化分析结果。

------

### 2️⃣ 核心能力抽取

只保留以下能力：

- RSS / 新闻 / 多源数据采集
- 文本清洗与标准化
- 热度/趋势计算逻辑
- LLM / NLP 情绪分析逻辑
- 标签分类 / 主题提取

------

### 3️⃣ 架构重构（重点）

- 数据采集，清洗，标准化能力集成到数据层

- AI接入能力集成到基础设施层，不仅可以用于本次的数据分析也可以以后复用在其他分析场景

要求：

- 完全独立于原项目运行
- 不依赖原项目服务
- 代码必须模块化
- AI所有输出必须标准化为JSON

------

### 4️⃣ 统一数据结构设计（必须）

设计一个统一输出格式，用于量化系统：

```
{
  "timestamp": "",
  "source": "",
  "asset_tags": [],
  "sentiment_score": 0.0,
  "attention_score": 0.0,
  "topics": [],
  "raw_text": ""
}
```

------

### 5️⃣ 与量化系统对接设计

说明如何与指数量化系统集成：

- 如何变成因子
- 如何进入策略层
- 更新频率建议
- 是否事件驱动

------

### 6️⃣ 代码输出要求

请严格遵守：

- 输出重构后的目录结构
- 输出关键模块代码（不是全部复制）
- 标注每个模块作用
- 去除UI / dashboard / 无关服务
- 不允许保留原项目冗余结构

------

## 约束条件

- 不要复制原仓库结构
- 不要包含前端代码
- 不要包含通知/推送系统
- 只保留“数据 + NLP + 因子生成”
- 必须面向量化系统可用性设计
- 输出必须工程化，而不是demo代码

------

## 最终目标

生成一个可以直接嵌入量化系统的模块：

输入：新闻 / RSS / 热点文本
输出：可用于策略的量化因子
