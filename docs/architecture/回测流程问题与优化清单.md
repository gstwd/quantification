# 回测流程问题与优化清单

本文记录 2026-09-13 对 `mainline_rs_rotation_v8` 做白板复盘与优化时**实际撞到**的问题，
以及从这些问题里提炼出的回测流程可优化点。每条都带现场证据；已修复的标注修复位置，
未修复的给出建议与优先级。

**修复进度**：A 类（P0/P1 口径与校验）、B 类（队列与执行吞吐）、C 类
（回测口径与结果可信度）与 D 类（研究流程与工具链）已全部修复，
修复实现与回归测试见各条目下的"修复"小节。
**F 类**（2026-09-14 第二轮 v8 优化新发现，共 18 条）**已全部修复**，
修复位置与验证见 F 节开头的对照表。
**G 类**（2026-09-14 平台逐参数审计，共 10 条）**已全部修复**，
含两处会产出错误绩效数字的正确性缺陷，见文末 G 节。

背景工程记录见 [`docs/research/主线RS轮动v8/README.md`](../research/主线RS轮动v8/README.md)。

优先级：**P0** 会直接产出错误结论；**P1** 严重影响可用性/吞吐；**P2** 效率与体验。

---

## A. 已修复（本轮改动，均有回归测试）

### A-1 [P0] `robustness scan` 浅拷贝别名导致整批变体失效

- 现象：`robustness scan --strategy mainline_rs_rotation_v8` 直接失败并返回
  `{"error": "未派生任何有效变体（候选配置均未通过校验）"}`。
- 根因：`build_knob_variants()` 用 `dict(config)` **浅拷贝**，再通过 `_set_leaf()` 就地下标写值
  （`services/robustness_service.py:890` 与 `:1061`）。v8 配置里 `risk.max_portfolio_exposure=1`
  按"整数"档位规则被扰动成 `2`（越界），这个越界值因为共享嵌套字典而**泄漏到全部候选乃至基线
  config 对象**，于是 33 个变体全部校验失败；同时 `baseline_config_hash` 是在污染之后计算的，
  落库的基线指纹也是错的。
- 修复：`create()`、`build_knob_variants()`、`build_ablation_variants()` 全部改为 `deepcopy`；
  新增 3 个回归测试（不得改写输入配置、单个越界档位不得泄漏、消融同样不得就地改写）。
- 影响面：任何"数值叶子中有越界扰动"的策略都会踩到，属于静默失效（只报一句笼统错误）。

### A-2 [P0] 负整数阈值被抬成正数

- 现象：`filters.rules[i].value = -5`（`return_5d > -5`）的邻域扰动产生 `+1` 而不是 `-6`。
- 根因：`_knob_values()` 对所有 int 一律 `step = max(1, ...)`、再 `max(1, value - step)`
  （`services/robustness_service.py:1016`），把"周期/持仓数至少为 1"的规则错误地套到了
  可以是负数的阈值上。
- 修复：负数整数阈值按 `value ± step` 扰动，不再抬到正数；测试覆盖 `{-6, -4}`。

### A-3 [P1] 列表内数值从未参与扰动

- 现象：v8 的过滤阈值（`filters.rules[2].value`）在稳健性扫描里从未出现，"阈值是不是平台"
  无从检验。
- 根因：`iter_numeric_leaves()` 不遍历 list 节点（`services/robustness_service.py:982`），
  而 `_set_leaf()` 也只支持点号路径，遇到 `rules[2]` 会抛 `KeyError` 并被静默跳过。
- 修复：`iter_numeric_leaves()` 支持列表下标（`filters.rules[2].value`），`_set_leaf()` 增加
  `name[index]` 解析（`_split_index`）。

---

## B. 队列与执行吞吐（已修复，2026-09-14）

> 修复实现集中在 `infra/job_queue/`（lane 并发预算、心跳、协作取消、僵尸扫描）、
> `api/routers/queue.py`（队列观测）、`api/routers/backtests.py` + `services/backtest_service.py`
> （状态过滤、排队可观测、取消）、`services/robustness_service.py`（批次取消/暂停、部分汇总）、
> 迁移 `0046_queue_observability`（timestamptz + 队列元数据）。
> 回归测试：`tests/unit/test_job_queue.py`、`tests/unit/test_robustness_control.py`。

### B-1 [P1] 服务端并发跑回测反而比串行慢 20 倍

- 现象：稳健性批次入队后（scan 136 + pool 44 + ablate 36 + 对照组 15 ≈ 231 条回测），
  队列吞吐长期停在 **0.3~0.7 条/分钟**；4 个 running 任务的已耗时超过 6 分钟仍未结束。
  同一时段用 CLI 同步跑一条 2 年回测只要 **17 秒**（空闲时 1 年回测 8 秒）。
- 证据：`background_job` 4 running 的 `started_at` 推算耗时 > 6 分钟；
  `pg_stat_activity` 显示 4 个会话卡在 `Lock/transactionid` 与 `Client/ClientRead`，本机 CPU 占用
  仅 0~3%（说明不是本机算力瓶颈）。
- 判断：`JobQueue` 在**单进程内开 4 个线程**（`infra/job_queue/queue.py`），回测是 CPU + 数据库
  混合型任务，线程并发既拿不到 GIL 收益，又把 4 份数据库负载叠加在一起，结果单条变慢、
  总吞吐下降。
- 修复：
  - 队列按 lane 划分，回测类任务（`backtest`/`comparison`）独占 **回测 lane**，与摄取/因子等
    通用任务互不抢占 worker（B6 的并发预算隔离一并解决）；
  - 回测 lane 并发默认 **1（串行）**：`QUANT_ETF_JOB_QUEUE_BACKTEST_WORKERS`；通用 lane
    `QUANT_ETF_JOB_QUEUE_WORKERS` 默认由 4 降到 **2**（同一台的数据库连接预算）；
  - 支持**独立 worker 进程**：`QUANT_ETF_JOB_QUEUE_EMBEDDED=false` 让 API 只入队，
    再用 `python -m quant_etf_api.worker`（或 `cli queue worker`）单独消费，容量独立可调。

### B-2 [P1] 队列没有优先级 / 取消 / 暂停

- 现象：一次长批次会把后续所有任务排在队尾。本轮为了等 5 条候选复核回测，只能改用
  CLI 同步执行绕开队列（同步模式不经过队列）。
- 修复：
  - 优先级：`backtest run --priority N`、`robustness scan/ablate/pool --priority N`
    （`background_job.priority` 早已在认领排序中使用，现已在 CLI 暴露）；
  - 取消：`backtest cancel <id>`（`POST /backtests/{id}/cancel`）、
    `robustness cancel <batch>`（`POST /robustness/{id}/cancel`）。未开始的任务直接落
    `cancelled`；运行中的任务只打 `cancel_requested` 标记，由回测主循环在安全检查点
    （每日循环起点，取消标记有 5 秒 TTL 缓存）协作退出，已提交的日结果保留；
  - 暂停：`robustness pause/resume <batch>`（`POST /robustness/{id}/pause|resume`），
    pending → paused → pending，暂停期间不被认领；
  - 批次号写入 `background_job.batch_id`，稳健性批次与优化会话的回测任务都能整批控制。

### B-3 [P1] 队列与单条回测的耗时不可观测

- 现象：判断"卡住了还是在跑"只能手工连库查 `pg_stat_activity`、`background_job`。
- 修复：
  - 新增 `GET /api/queue/stats`（积压/吞吐/运行中任务/并发预算）与
    `GET /api/queue/jobs`（任务明细，含心跳时间与已耗时），CLI 对应
    `cli queue stats|jobs`；
  - `GET /backtests` 每条回测附带 `job_status` / `queued_seconds` / `elapsed_seconds` /
    `queue_position` / `job_priority` / `batch_id`（回测任务统一去重键
    `backtest:{id}`，据此反查队列任务）；
  - 前端：回测中心新增"队列"列与取消按钮，运行记录页新增队列概览面板。

### B-4 [P2] `GET /backtests` 不支持状态过滤

- 现象：`limit=200` 的一条查询结果全是 `pending`，看不到已完成记录。
- 修复：`GET /backtests` 新增 `status` / `purpose` / `order_by`(created_at|started_at|
  finished_at) / `descending` 参数（`strategy_id`、`created_from/to` 原有），CLI 对应
  `backtest list --status/--purpose/--order-by/--asc/--limit`；前端回测中心新增状态与
  用途下拉筛选。

### B-5 [P1] 应用时间戳与数据库 `now()` 时区混用

- 现象：用 SQL 计算 `now() - started_at` 得到 8 小时误差（应用写 UTC 的 naive 时间戳，
  `now()` 返回会话本地时间 = UTC+8）。本轮诊断里差点据此误判"任务卡了 8 小时"。
- 修复：迁移 `0046_queue_observability` 把 `background_job.*`、
  `backtest_run.created_at/started_at/finished_at`、
  `backtest_comparison.created_at/started_at/finished_at` 统一改为 `timestamptz`
  （存量 naive 值按 `AT TIME ZONE 'UTC'` 解释），应用侧统一用
  `infra.time.utcnow_aware()` 写入 aware UTC；回测列表按中国日期过滤时也改为传 aware
  下界。前端仍按北京时间展示。

### B-6 [P2] 外部数据摄取与回测争抢同一个远程库

- 现象：本轮吞吐崩塌期间，`pg_stat_activity` 里持续出现 `INSERT INTO stock_moneyflow`、
  `INSERT INTO stock_daily_close`、`SELECT DISTINCT ... stock_daily_basic` 等个股摄取语句，
  与回测会话互相拖慢。
- 修复：随 B-1 的 lane 隔离一并落地——回测 lane 的并发即"回测可用的数据库并发预算"
  （默认 1 条），摄取/因子等任务在通用 lane，两者不再叠加；独立 worker 进程部署时
  还能把两者放到不同进程/不同容量配置，必要时进一步分离实例。

### B-7 [P1] 卡死的 running 任务会永久阻塞批次汇总

- 现象：`ablate` 批次 35/36 成功、`pool` 批次 43/44 成功，各有一条回测长期停留在
  `running`（同一时段其它回测正常完成，队列整体在推进）；`robustness collect` 要求整批
  完成，于是官方汇总迟迟拿不到。
- 现场：`backtest_run` 中 `ef250c62-8965-4a7e-841a-50514f030ba7`（基线 2016-01-04~2018-06-28）
  与 `afc54a73-8aa1-4641-9b63-ecae5a172997`（消融变体 2020-12-25~2023-06-29）已 `running`
  **61.8 分钟**，而同时段其它同长度窗口的扫描变体只要 0.3~0.7 分钟。
- 根因：`recover_stuck_jobs()` 只在进程启动时执行一次，运行期没有心跳、没有单任务超时、
  没有僵尸任务扫描；一条卡住的回测会一直占用 worker，并让整个批次无法收口。
- 修复：
  - `background_job.heartbeat_at`：worker 执行期每 15 秒写一次心跳
    （`QUANT_ETF_JOB_HEARTBEAT_INTERVAL_SECONDS`）；
  - 运行期僵尸扫描线程（`QUANT_ETF_JOB_ZOMBIE_SCAN_ENABLED`）：把心跳超过
    `QUANT_ETF_JOB_STUCK_TIMEOUT_SECONDS`（默认 1800 秒）未更新的任务回收；
    即使心跳正常，运行超过 `QUANT_ETF_JOB_MAX_RUNTIME_SECONDS`（默认 7200 秒）的长任务
    同样回收，覆盖"卡在数据库锁上"的场景；
  - 回收时回调 `JOB_ABANDON_HANDLERS`，把对应 `backtest_run` / `backtest_comparison`
    一并落为 failed（附 `JOB_ABANDONED` 结构化提示），批次因此可以收口；
  - `robustness collect --allow-partial`：跳过未完成/失败窗口，按已完成窗口汇总，并把
    `coverage`（expected/completed/pending/failed/is_partial）写入 summary，批次状态落
    `partial`；`cancel` 会把批次内任务已被取消、永远不会再跑的 pending 回测直接落为
    cancelled，运行中的回测则走协作取消路径（主循环退出后自行落 cancelled）。
- 验证：现场那两条卡死回测已被扫描回收（`scan_zombies` 回收 2 条），ablate 批次用
  `--allow-partial` 汇总得到 `coverage={expected:36, completed:35, failed:1}`、状态 `partial`。


---

## C. 回测口径与结果可信度（已修复，2026-09-15）

> 修复实现集中在 `domain/common/trading_calendar.py` + `infra/trading_calendar.py`
> （交易日历严格口径）、`domain/strategies/rebalance.py`（日历必填、取消降级）、
> `domain/portfolio/turnover.py`（换手计入清仓/建仓腿）、
> `domain/research/stability.py`（多档成本并列）、
> `services/backtest_reference_service.py`（悬挂引用检测与清理）、
> `services/backtest_service.py`（口径指纹、候选池时间线、受控删除）、
> 迁移 `0047_backtest_candidate_pool` + `0048_backtest_reference_integrity`。
> 回归测试：`test_trading_calendar_strict.py`、`test_rebalance_calendar.py`、
> `test_backtest_turnover.py`、`test_backtest_span.py`、`test_backtest_references.py`、
> `test_backtest_candidate_pool_timeline.py`、`test_backtest_repository_filters.py`、
> `test_calendar_http_errors.py`。

### C-1 [P0] 周度调仓的交易日历静默降级，会产生两套结果

- 现象：同一配置在平台与"按真实交易日历复现"的本地引擎上，2022-2023 段累计收益差 1.2pp、
  2024-2025 段差 2.5pp，且差异全部集中在**长假后第一周**。
- 根因：`DefaultRebalanceScheduler` 在交易日历不可用时降级为 `WeekendFallbackCalendar`
  （`domain/strategies/rebalance.py:58`、`domain/common/trading_calendar.py:40`），
  只按星期判断交易日。于是"目标星期（周三）恰好是法定节假日"的那一周，会把节假日当成交易日，
  导致整周跳过调仓（实测 2022-05-05 之后、2025-01-02 之后各有若干天）。
  更关键的是：`BacktestService.__init__` 与 `StrategyDecisionService` 构造调度器时
  **根本没有注入日历**，所以回测恒定跑在周末近似日历上。
- 影响：结果不可复现地依赖"当时 akshare 是否可用"；降级只写一条 warning 日志，回测结果里
  **没有任何痕迹**——同一 `config_hash` 的两条回测可能代表两种调仓日历。
- 修复（严格口径，不再有任何近似路径）：
  - **彻底删除 `WeekendFallbackCalendar`**；`TradingCalendar` 的
    `is_trading_day` / `latest_trading_day` / `next_trading_day` / `trading_days_between`
    在取不到日历时一律抛 `TradingCalendarUnavailableError`（消息含修复指引）；
  - 日历解析统一走 `resolve_trading_calendar(db, required_range=...)`：
    **上游数据源（Tushare 优先 → AkShare）→ 本地 `trading_calendar` 表快照 → 报错**，
    两种来源都是真实日历，不是按星期推算；
  - `DefaultRebalanceScheduler(trading_calendar)` 日历改为**必填位置参数**，
    并删除"日历异常时降级为按星期比较"的 `except` 分支；
  - 回测侧：周度/月度调仓解析失败 → 回测直接落 `failed`；每日调仓与日历无关，
    指纹记为 `not_required`；来源写入 `params["_calendar_source"]`（`upstream` / `database`
    / `not_required`）并透出到 `stability.calendar_source`，使用本地快照时给出
    `CALENDAR_SOURCE_DATABASE` 信息级提示；
  - 上游加载失败只做 **60 秒** 负缓存，避免一次瞬时网络故障把日历冻结一整天；
  - 调用点按三类语义处理：后台任务/CLI **上抛落失败**、调度器线程**拒绝本次执行并记错误**、
    HTTP 读端点返回 **503**（`GET /ai-factors/previous-trading-day`、
    策略分配等；显式传 `trade_date` 可绕过日历）；
  - 新增审计入口：`GET /backtests?calendar_source=`、`cli backtest list --calendar-source`，
    用于找出"跑在另一种日历口径上"的存量回测。
- 验证：`tests/unit/test_trading_calendar_strict.py`（优先级、抛错、负缓存、无降级类）、
  `tests/unit/test_rebalance_calendar.py`（指纹、fail-fast、提示）、
  `tests/unit/test_calendar_http_errors.py`（503）。现场实测：新回测的
  `stability.calendar_source` 为 `upstream`（AkShare 可用时）或 `database`
  （上游不可用时回退本地 8797 行日历快照，并带 `CALENDAR_SOURCE_DATABASE` 提示），
  `grep -rn "WeekendFallbackCalendar" apps/` 已无结果。

### C-2 [P1] 换手率不计清仓 / 建仓腿

- 现象：调仓日 `prev` 或 `new` 为空时换手记为 0（`services/backtest_service.py:799`
  `if prev_positions and new_positions`）。v8 在 2016-2017 段有 13 天属于"清仓型调仓"，
  其换手成本被系统性记为 0。
- 影响：净口径指标低估成本，方向上偏向"高换手策略"。
- 修复：
  - 调仓日统一按 `Σ|Δw| / 2` 计入（`domain/portfolio/turnover.py::compute_turnover`），
    删除主循环里的"一侧为空则记 0"守卫；建仓腿与清仓腿全额入账；
  - 新增换手口径指纹：新回测写 `params["_turnover_model"] = delta_w_v2`，
    存量回测读取时标注为 `legacy_v1`（`stability.turnover_model`），前端对 legacy
    显示"历史换手口径，净口径需重跑后可比"。
- 验证：`tests/unit/test_backtest_turnover.py`（清仓/建仓日换手落库、指纹）、
  `tests/unit/test_domain_layering.py::test_compute_turnover_includes_open_close_legs`。
  现场实测：新回测 `stability.turnover_model == "delta_w_v2"`，存量 243 条回测
  全部读为 `legacy_v1`（前端会提示净口径需重跑后可比）。

### C-3 [P1] 净口径成本档位必须重跑回测

- 现象：`params["_cost_bps"]` 在创建回测时固化，想看 20bp/30bp/50bp 只能新建回测
  （或像本轮一样自己读逐日序列重算）。
- 修复：
  - `domain/research/stability.py::compute_cost_ladder`：只重算与成本有关的量
    （成本序列、累计/年化/夏普/超额），不重算年集中度/分段/回撤 → O(档位数 × 交易日数)，
    读取路径零可感额外成本；
  - `stability.cost_ladder` 始终并列输出系统配置的档位（默认 `0/10/20/30/50`，
    0 = 毛口径），档位由 `QUANT_ETF_STABILITY_COST_LADDER` 配置；
  - `GET /backtests/{id}?cost_bps=` 与 `cli backtest show --cost-bps/--cost-ladder`
    可按需覆盖主口径与档位，**不落库、不重跑**。
- 验证：`tests/unit/test_research_stability.py::TestCostLadder`、
  `tests/unit/test_backtest_turnover.py::test_ladder_zero_bp_equals_gross`。
  现场实测：`backtest show <id> --cost-ladder 0,10,50` 输出三档；`0bp` 档净累计
  与 `metrics.cumulative_return_pct` 一致；`?cost_bps=50` 覆盖后的
  `stability.net_annualized_return_pct` 与 50bp 档位值**完全相等**（并同步进
  `metrics.net_*`）。

### C-4 [P1] 分段回测缺少官方"拼接全期"口径 → 跨度限制已取消

- 现象：规则要求总跨度 > 5 年必须分段，但分段之后没有把各段日收益串成全期指标/净值的接口；
  本轮只能自己按日期拼接（`analyze_costs.py --aggregate`）。
- 修复：**取消"总跨度 > 5 年必须分段"的流程约定**——研究期内任意跨度
  （1 个月 ~ 研究期全段 10 年）都可以单次回测，不再需要任何拼接接口。
  - 事实核对：后端从未存在跨度上限或强制分段校验（`create_backtest` 只校验
    `start <= end` 与研究期/验证期边界，交易日由行情可用区间自然收敛），
    该限制只写在文档与 E 节流程约定里，因此修复是"改写约定 + 锁定行为 + 提示文案"；
  - CLI `backtest run --start` 帮助文本与前端"回测区间"提示明确"任意跨度均可，无需分段"；
    长区间的阶段视角由既有分年度绩效与三段一致性给出；
  - 研究/验证期边界规则**未放宽**：研究类回测仍不得越过研究期末端（2025-12-31），
    跨界需显式 `purpose=validation`（任意跨度 ≠ 任意用途）。
- 验证：`tests/unit/test_backtest_span.py`（1 个月 / 2 年 / 3 年 / 10 年均可创建且只建一条记录；
  越界仍拒绝）。现场实测：`mainline_rs_rotation_v8` 以
  `--start 2016-01-01 --end 2025-12-31`（研究期全段 10 年）**单次跑通**，
  返回 2016~2025 共 10 个自然年的分年度绩效，全程未分段；实测单次 10 年回测
  （2431 个交易日、21 个标的）耗时约 5 分钟，独占回测 lane，可用 `queue stats` 观测。

### C-5 [P2] 删除回测记录会留下悬挂引用

- 现象：用户清空 `backtest_run` 后，`robustness_run.variants[].backtest_ids`、
  `strategy_optimization`（42 行）、`strategy_lifecycle.research_backtest_id`（1 行指向已删除回测）
  都指向不存在的回测。
- 现场统计（修复前实测）：优化会话 `baseline_backtest_id` / `candidate_backtest_id`
  各 41 条悬挂；生命周期 `research_backtest_id` / `validation_backtest_id` 各 1 条；
  `fold_backtests` 内 452 条；`robustness_run.variants` 内 6 条；
  日结果/指数结果孤儿行 0 条。
- 修复：
  - 迁移 `0048_backtest_reference_integrity`：先把结构性悬挂引用清理干净
    （可空引用置 NULL、失去父回测的明细删除），再重建外键语义——
    `backtest_daily_result` / `backtest_index_result` / `backtest_comparison`
    → `ON DELETE CASCADE`；`strategy_optimization.{baseline,candidate}_backtest_id`、
    `strategy_lifecycle.{research,validation}_backtest_id` → `ON DELETE SET NULL`；
  - JSONB 引用（稳健性变体窗口、优化折窗口）无法加外键，改为**可检测 + 可标记 + 可清理**：
    `GET /backtests/orphans` / `cli backtest orphans` 审计悬挂引用；
    `cli backtest prune-dangling-refs [--apply]`（默认预演）移除悬挂窗口并把标签写入
    `variants[*].deleted_windows`、把折窗口引用置 `null`；
  - `robustness collect` 不再把"已删除的回测"当成"仍在排队"——单独计入
    `coverage.missing_windows`，否则批次会永远停在 `running`（与 B7 同类失效）；
  - 受控删除：`DELETE /backtests/{id}?force=`、`cli backtest delete <id> [--force]`，
    运行中的回测必须先取消；存在 JSONB 引用时默认拒绝（409）并列出持有者；
  - 可见性：`RobustnessDetail.missing_backtest_ids`、`optimization show` 的
    `missing_backtest_ids` 与报告中的"⚠ 缺失引用"小节、生命周期页对空回测引用
    显示"未记录（或已被删除）"。
- 验证：`tests/unit/test_backtest_references.py`（检测/清理/删除守卫/批次缺失语义）。
  现场实测：迁移 `0048` 的预清理把优化会话 41+41 条、生命周期 1+1 条悬挂引用置空，
  孤儿明细 0 条；`backtest prune-dangling-refs --apply` 清理稳健性窗口 6 条 +
  折引用 452 条（写入 `deleted_windows` / 置 `null`）后，`backtest orphans` 总数由
  **458 → 0**；受控删除对"仍被稳健性变体引用"的回测返回 409 并列出持有者，
  对未被引用的回测删除成功（日结果级联清理）。

### C-6 [P2] 回测产物缺少"有效候选池"的时间线

- 现象：v8 的 21 个池成员里，`000688` 2016-2019 完全无行情、`931160`/`931743` 2016-2017
  无行情，`930601`/`931152`/`931494` 早期缺开盘价（T+1 开盘执行不可用）；回测只在
  `warnings` 里给一条汇总提示，看不出"早期实际可交易池只有 13~16 个"。
- 修复：
  - 主循环复用每日候选池计算，累积逐日规模并做**游程编码**（连续相同规模合并），
    随 `mark_success` 写入新增列 `backtest_run.candidate_pool`（迁移
    `0047_backtest_candidate_pool`）；
  - 结构含 `base_size / min_size / max_size / median_size / pool_coverage_ratio /
    segments / exclusions / truncated_exclusions`；剔除明细按交易日数降序截断
    （`QUANT_ETF_CANDIDATE_POOL_EXCLUSION_LIMIT`，默认 50）；
  - 池规模缩水时追加信息级告警 `CANDIDATE_POOL_SHRINK`（含最小规模区间）；
  - 读取路径透出到 `stability.candidate_pool`，CLI `backtest pool <id>` 输出游程段与
    剔除区间表，前端回测详情页新增"有效候选池"区块。
- 验证：`tests/unit/test_backtest_candidate_pool_timeline.py`。现场实测
  `mainline_rs_rotation_v8`（2016-01-01 ~ 2025-12-31）：理论池 21，
  逐日有效池 **13 → 10 → 11 → … → 21**（中位 14），池覆盖率 0.7231，
  16 个游程段、21 条剔除区间（如 `931743` 被剔除 1829 个交易日），
  并产出 `CANDIDATE_POOL_SHRINK` 信息级提示——正是"早期实际可交易池只有
  13~16 个"的量化证据。

---

## D. 研究流程与工具链（已修复，2026-09-14）

> 修复实现集中在 `services/research_batch_service.py`（不落库批量评估）、
> `services/backtest_service.py`（`persist=False` + 跨变体共享缓存）、
> `services/robustness_service.py`（旋钮优先级 / 预设 / `scan_params`）、
> `services/strategy_config_service.py`（变体清理、验证期留痕）、
> 迁移 `0049_research_tooling`、CLI `research batch` / `robustness scan --knobs`
> / `strategy prune-variants` / `strategy consume-validation`，
> 以及前端策略详情页"稳健性验证"页签。
> 回归测试：`tests/unit/test_research_batch.py`、`test_strategy_variants.py`、
> `test_research_robustness.py::TestKnobPriorityAndPresets`。

### D-1 [P1] 缺少"批量变体 → 秒级评估"的官方路径

- 现象：探索性诊断（消融 9 个 + 池扰动 9 个 + 参数 15 个 + 简化 6 个 ≈ 40 个变体 × 5 段）
  在平台上等于 200 条回测，在当前吞吐下需要数小时；本轮只能在研究目录里复刻引擎
  （`v8_local.py`，换手/仓位与平台完全一致、收益差 < 1pp）才把成本压到秒级。
- 根因：(a) 每条变体都要落库（逐日结果 + 逐指数结果，单条 2 年 ≈ 5 万行）；
  (b) 同一窗口的行情加载与因子预计算在每个变体上重复一遍。
- 修复：新增 `research batch` 命令组与 `ResearchBatchService`，
  **复用与平台完全相同的执行路径**（`_run_backtest_loop(persist=False)`），
  只关闭结果落库与状态提交，并按窗口共享行情快照与因子预计算：
  - `cli research batch --strategy <基线> --variants variants.json --windows 5 [--cost-bps] [--cost-ladder]`；
  - 变体文件支持完整 `config` 或 `patch`（点号 / 列表下标路径，如
    `filters.rules[1].value`），patch 的路径不存在会直接报错而不是静默跳过；
  - 输出逐窗口毛口径 + 净口径指标、多档成本并列、`vs_baseline`（Δ净年化 / Δ净夏普 /
    劣化窗口占比），以及固定口径指纹（执行模型 / 数据质量 / 基准 / 日历来源 /
    换手口径 / 成本 / 区间）；
  - **不落库即不可审计**：输出里 `caliber.persisted=false`，验收仍必须走 `backtest run`。
- 为什么不是"再写一个引擎"：C-1（周末近似日历）与 B16（warn/strict 候选池分叉）都是
  "两套实现、两套结果"造成的；批量路径与平台共用同一条代码路径，回归测试
  `test_metrics_match_persisted_path` 锁定"落库与不落库两条路径的逐日收益、换手、
  仓位完全一致"。

### D-2 [P1] `robustness scan` 的默认规模与实际吞吐不匹配

- 现象：默认 `--max-knobs 30` × `--windows 4` = 136 条回测；更糟的是截断按**路径字母序**，
  默认 30 档时 `timing.thresholds.*`（最可疑的拟合参数）永远排在最后被截掉。
- 修复：
  - 截断顺序改为**业务重要性**（`_KNOB_PRIORITY`）：择时阈值 → 过滤阈值 → 评分权重 →
    风险 / 仓位 → 调仓；同优先级内仍按路径排序，保证可复现；
  - 支持**关键旋钮清单**：`--knobs timing.thresholds.offensive_score,filters.rules[1].value`
    或 `--knobs-file knobs.json`，只扫清单内字段；清单里的路径不存在时直接报错；
  - 新增**轻量体检预设** `--preset quick`（2 窗口 / 8 旋钮）与 `--preset standard`
    （4 / 30，与旧默认一致）；显式 `--windows/--max-knobs` 优先于预设；
  - 扫描口径随批次落库（`robustness_run.scan_params`：preset / knobs / windows），
    前端批次详情与 `robustness show` 都能看到"这批到底扫了什么"；
  - `--preset quick` 已写进 strategy-optimizer 的技能流程与验收清单（第 6 项）。

### D-3 [P2] 稳健性结果没有进入前端

- 现象：`robustness` 只有 CLI + 只读 API，页面上看不到 PBO/DSR/邻域结论，
  人工复核要开终端。
- 修复：策略详情页新增"稳健性验证"页签（`/strategies/{id}` → 稳健性验证）：
  - 批次列表（类型 / 状态 / 试验数 / 窗口 / 创建时间），运行中的批次可直接取消；
  - 批次详情：扫描口径、覆盖率（部分汇总会显式标注"结论仅供参考"）、
    参数邻域稳定度（Δ 范围 / 劣于基线占比 / 是否平台 / 是否方向反转）、
    消融边际贡献表、池扰动分布、PBO / Deflated Sharpe / 自助法置信区间、
    变体逐项结果表；
  - 配套 `apps/web/src/api/robustness.ts` 与 `GET /robustness?strategy_id=`。

### D-4 [P2] 稳健性变体策略的清理没有元数据支撑

- 现象：一次 scan 会批量创建 `<base>__rbXXXX_*` 形式的 draft 策略（本轮 33 个），
  清理时只能按 ID 前缀猜。
- 修复：
  - `strategy_config` 增加 `is_variant` 与 `source_batch_id`（迁移 `0049` 已按
    `description` 中的批次号回填存量 50 个变体），稳健性服务创建变体时自动写入；
  - 新增 `cli strategy prune-variants --batch <批次ID>`：
    默认**预演**（`--apply` 才真删）；变体仍被回测引用时跳过并列出引用，
    `--force` 才连带删除这些回测；
  - **不删除稳健性批次本身**：批次是 Deflated Sharpe 的"试验次数 N"台账，
    删掉变体策略不等于抹掉历史试验。
- 验证：现场 `strategy prune-variants --batch 28e7564b…` 预演命中 33 个变体策略。

### D-5 [P2] 验证期留痕只覆盖回测，不覆盖"研究者的眼睛"

- 现象：v8 的历史版本说明显示 v1.4~v1.6 都在 2010-2026 全区间（含 2026 验证期）上选参数，
  但 `backtests/validation-usage` 只能列出"跑过验证期回测"的记录，无法标记
  "该策略的验证期已被消费"这一状态。
- 修复：
  - `strategy_config` 增加 `validation_consumed_at` / `validation_consumed_note`
    （迁移 `0049` 按"2026-01-01 起存在回测"回填存量事实）；
  - 创建 `purpose=validation|monitor` 回测时**自动首次留痕**（记录回测 ID 与用途说明），
    再次命中只追加说明、不覆盖首次时间；
  - 人工途径：`cli strategy consume-validation <策略> --note "..."`；
  - 可见性：`GET /backtests/validation-usage` 每条记录并列输出该策略的
    `strategy_validation_consumed_at` / `..._note`，`strategy show` 也直接可见；
  - 语义不变：验证期数据只能用于否决，用于确认一律无效。

---

## E. 本轮实际操作中值得保留的做法

1. **先对齐再分析**：本地复现必须先与平台逐段对齐（换手/仓位完全一致、收益差 < 1pp）
   才允许用来下结论；对齐过程本身就是发现 C-1 的方式。
2. **分段是可选的分析视角**（C-4 后不再是流程强制）：研究期内任意跨度都可单次回测；
   想逐段观察时，按自然年/等分段看分年度绩效与三段一致性即可，不需要拼接接口。
3. **成本四档并列**：毛 / 10bp / 20bp / 30bp / 50bp 一起看，换手 13 倍的策略只看毛口径会得出
   相反结论；现在这四档在回测详情里直接并列（`stability.cost_ladder`，C-3）。
4. **区分"探索"与"验收"**：探索用本地引擎（秒级、可跑几十个变体），验收用平台回测
   （正式口径、落库可审计）。
5. **同步 CLI 作为队列的旁路**：队列积压时用 `backtest run`（默认同步）绕开队列，
   单条 17 秒即可拿到正式口径结果。

---

## F. 第二轮 v8 优化（2026-09-14）新发现的问题（已修复）

第二轮用 A~D 修复后的工具链（`research batch` / `robustness scan+collect+stats` /
`strategy prune-variants` / 研究期边界）重跑 v8，撞到下面这批问题。
它们大部分**不影响单条回测的正确性，但会直接扭曲稳健性结论**，
所以按"会产出错误结论"的严重程度排序。

### 修复对照表

| # | 问题 | 修复位置 | 验证 |
| --- | --- | --- | --- |
| F-1 | 汇总按不同窗口集相减 | `RobustnessService._summarize`（共同窗口集 + `coverage.comparable`） | `tests/unit/test_f_robustness_fixes.py::TestCommonWindowComparison`；现场重算批次 `267c4a9c` 池扰动中位 Δ 由 −0.3096 → **−0.0056**、`no_late_listed` 由 −0.1719 → **+0.1579** |
| F-2 | 消融标签重名静默复用变体 | `build_ablation_variants` 标签带规则下标；`_create_variant_strategy` 复用前比对配置哈希，不一致直接报错；`create` 增加标签唯一性校验 | `test_research_robustness.py::TestVariantGeneration::test_ablation_labels_unique_for_same_factor_rules` |
| F-3 | 空批次 `is_plateau=true` | `domain/research/robustness.summarize_neighborhood` 无变体时返回 None；前端显示"有效变体不足，无法判定" | `test_research_robustness.py::TestNeighborhood::test_empty_variants`、`test_f_robustness_fixes.py::test_empty_variant_results_do_not_report_plateau` |
| F-4 | 批次状态无法收口 | 新增 `robustness abandon`（CLI + `POST /robustness/{id}/abandon`）；`collect` 区分 `missing_windows`（回测已删除）与 `failed_windows`；列表/详情新增 `is_stale` | `test_f_robustness_fixes.py::TestCollectWindowClassification/TestAbandonAndStale`；现场 `robustness abandon` 返回 `cancelled_jobs=5` |
| F-5 | 同步批次行不可见 | `create()` 先落库批次行（含变体映射）再执行，逐变体回写并提交 | 现场：`robustness list` 在批次执行期即可见（status=running） |
| F-6 | 预热/缺失口径放大单点缺口 | `_estimate_warmup_trading_days` 按"各指数自身首根 K 线"起算；`_collect_missing_factor_warnings` 按指数排序给出明细 | `test_f_backtest_fixes.py::TestWarmupPerAsset/TestMissingFactorWarningDetail`；现场 v8 短区间回测的 WARMUP 由 1038 天（十年口径）降为按资产口径计数 |
| F-7 | 非交易日行情被当交易日 | 回测侧 `_filter_non_trading_dates` + `NON_TRADING_BAR` 警告；摄取侧 `IngestService._drop_non_trading_bars` | `test_f_backtest_fixes.py::TestNonTradingDateFilter/TestIngestNonTradingGuard`；现场回测输出"已从回测交易日中剔除: [2018-06-18]" |
| F-8 | 缺开盘价静默剔除 | 新增 `EXECUTION_PRICE_MISSING` 警告（`_execution_price_warning`），列出指数与天数 | `test_f_backtest_fixes.py::TestExecutionPriceWarning`；现场 v8 2018 段提示"8 个指数因缺开盘价被排除（合计 608 个交易日）" |
| F-9 | 缺排名摘要、吞吐口径 | `ResearchBatchResult.to_summary_table()` + `research batch --summary` | 现场输出变体 × 指标表（含净口径、Δ、逐窗口 Δ） |
| F-10 | 拼接口径不可比 | `research_batch_service._caliber_digest` 增加 `aggregate_mode=window_stitched`；摘要表首行标注 | 现场 `--summary` 表头含"aggregate 为窗口拼接口径，不可与整段回测直接比较" |
| F-11 | 详情 `index_codes` 为空 | `StrategyConfigService.get_config` 从 `config_json` 回填 | `test_f_platform_fixes.py::TestStrategyDetailIndexCodes`；现场 `strategy show` 返回 21 个代码 |
| F-12 | 列表净口径恒为 null | `BacktestService.list_backtests(include_net=True)` + `BacktestRepository.find_return_turnover_pairs` 批量现算 | `test_f_backtest_fixes.py::TestListNetMetrics`；现场 `backtest list --status success` 返回净夏普/净年化/换手 |
| F-13 | 批次吞吐受单 worker 限制 | `robustness scan/ablate/pool` 新增 `--parallel N`（进程池本地并行，仅同步模式） | 现场 `--parallel 2` 两条 5 年回测并发完成（11:26:15 起、11:26:52/59 结束） |
| F-14 | PBO 在窗口不足时给 0 | `domain/research/robustness` 增加 `MIN_PBO_BLOCKS=4`，不足时 `pbo=None` + `reason`；`stats` 输出 `is_partial` | `test_research_robustness.py::TestPbo`；现场 `stats` 返回 `value=null, reason="分块数 2 < 4…"` |
| F-15 | `optimization show` 无验收清单 | `OptimizationService.show` 返回 `acceptance_checklist` | `test_f_platform_fixes.py::TestOptimizationShowChecklist`；现场会话返回 7 项 |
| F-16 | 会话净口径恒为 null | `OptimizationService._collect_metrics/_net_metrics` 并入净口径 | `test_f_platform_fixes.py::TestOptimizationNetMetrics`；现场 v1.7.0 候选回测净夏普 0.4512、年化换手 15.83 |
| F-17 | promote 不更新描述 | `finish(promote=True)` 追加 `_append_version_history` | `test_f_platform_fixes.py::TestPromoteVersionHistory` |
| F-18 | `optimization start --start` 缺省只覆盖 2 年 | CLI 缺省改为研究期起点 | `test_f_platform_fixes.py::TestOptimizationDefaultPeriod` |

> F-2 的历史批次（`e72997a1`）里"删 ma_10d"那一行是旧代码产出的重复配置，
> 已用修复后的口径重新汇总，但该行数据本身不可用；如需正式结论需用新代码重跑一次
> `robustness ablate`。

### F-1 [P0] 稳健性汇总把"窗口数不同的均值"直接相减，池扰动结论被整体反转

- 现象：批次 `267c4a9c…`（pool）里基线只覆盖 3 个窗口（`baseline/W0` 失败），
  10 个池扰动变体各覆盖 4 个窗口。平台汇总输出"**所有**变体都变差"：
  `delta_median=-0.3096`、`delta_min=-0.3906`、`delta_max=-0.1719`。
- 按共同窗口集（W1~W3）重算同一批数据：

  | 变体 | Δ夏普（平台口径） | Δ夏普（共同窗口） |
  | --- | --- | --- |
  | `no_late_listed` | −0.1719 | **+0.1579** |
  | `rand80_03` | −0.2621 | **+0.0733** |
  | `rand80_00` | −0.2959 | −0.0007 |
  | `rand80_07` | −0.3906 | −0.1236 |

  共同窗口下 Δ 分布 −0.12~+0.16、中位≈0，即"池内成员置换基本中性，
  去掉后上市指数反而更好"；平台口径则给出完全相反的结论。
- 根因：`RobustnessService._summarize()` 用 `baseline_sharpe_mean`（基线的有效窗口均值）
  直接减每个变体的 `sharpe_mean`，不做窗口集合交集；基线缺窗时两者不在同一时间样本上。
- 复算脚本：`docs/research/主线RS轮动v8/round2/recheck_batch.py`（只读）。
- 建议：汇总前取所有变体的**共同窗口集**；交集为空时 `delta_*` 返回 null 并置
  `coverage.comparable=false`，前端/报告同时展示窗口覆盖。

### F-2 [P0] 消融变体标签冲突 → 静默复用同一变体，一个消融结论被另一份配置冒充

- 现象：批次 `e72997a1…`（ablate）汇总里 `ablate_filter_close_price` 出现两条
  （`knob` 分别是 `filters.rules[0].close_price` 与 `filters.rules[1].close_price`），
  但 `sharpe_mean=0.4636`、`annualized_return_mean=7.62` **完全相同**；
  库里该批次只落了 **7 个**消融变体策略（5 因子 + 2 规则 + 2 规则本应 9 个候选）。
- 根因：消融标签 `ablate_filter_{_slug(factor)}` 只含因子名，两条 `close_price` 规则同名
  （`services/robustness_service.py:1234`）；`_create_variant_strategy()` 在
  `get_config(variant_id) is not None` 时**直接复用已有变体**
  （`services/robustness_service.py:857`），于是第二次消融把回测挂在第一次的配置上。
- 影响："删掉 `ma_10d` 会怎样"这一问其实从未被真正回测（跑的是删 `ma_17d`），
  而平台不报任何错；只有肉眼发现两条指标完全相同才能察觉。
- 建议：标签拼上规则下标（`ablate_filter_rules0_close_price`）；复用分支必须比对
  变体配置哈希与候选配置一致，不一致时报错而不是静默复用。

### F-3 [P1] 空批次/部分批次的 `neighborhood.is_plateau` 默认为 true

- 现象：批次 `28e7564b…`（scan）132/136 个窗口因"回测已删除"失败，
  `collect --allow-partial` 后汇总仍输出
  `n_variants: 0, delta_min: 0.0, delta_max: 0.0, worse_ratio: 0.0, reversal: false, is_plateau: true`。
- 风险：验收清单第 6 项（"参数邻域无方向反转"）会因此被读成**通过**。
- 建议：无有效变体时 `is_plateau=null`，并在汇总里强制并列 `coverage.completed/expected`。

### F-4 [P1] 批次状态不会自然收口，`running` 可以永久挂着

- 现象：两个批次自 2026-09-13 13:28 起 `status=running`，`pending` 任务为 0；
  用户的回测被删除后 132 个窗口变成 `回测已删除`，只有人工
  `robustness collect --allow-partial` 才能收口，且 `failed_windows` 把
  "用户主动删除"与"执行失败"混为一类。
- 建议：新增 `robustness invalidate/abandon`；`collect` 把 deleted 归入
  `missing_windows` 并标明原因；批次超过 N 小时无任何窗口进展自动标记 `stale`。

### F-5 [P1] 同步模式的批次行在整批跑完之前对其它会话不可见

- 现象：`robustness scan --sync` 已跑 50 分钟、已产出 15 条 `success` 回测，
  但 `robustness_run` 里查不到该批次（`robustness list` 最新仍是前一天的批次），
  前端"稳健性验证"页签看不到，也无法 `robustness cancel/pause`。
- 根因：同步路径把整批回测放在**同一个事务**里，批次行到全部结束才 commit；
  异步路径先 commit 批次再入队，所以可见。
- 建议：先 commit 批次行（含 windows/variants 映射）再执行；或每完成一个窗口提交一次进度。

### F-6 [P1] `MISSING_FACTOR`/`WARMUP` 的统计口径把单点缺口放大成"全池损坏"

- 现象（基线回测 `22666cd7…`）：
  "因子 `close_price` 在 **976** 个交易日存在缺失（2016-01-04~2019-12-31，涉及指数
  `['000300','000688','000813','000852','000905']` 等）"、
  "回测前 **1038** 个交易日长周期因子数据不足（预热期）"。
- 实际：只有 `000688`（行情自 2020-01-02 才有）一只指数缺数据，
  976/996/1038 天全部等于这一只的缺口长度；"涉及指数"只是**字母序前 5 个**，
  于是读起来像全池数据损坏、像前 4 年都在预热。
- 根因：`_collect_missing_factor_warnings()` 按"任一指数缺失即计一天"计数
  （`services/backtest_service.py:2287`）；`_estimate_warmup_trading_days()` 用
  "**全部**指数均有有效值"定义预热起点（`:2399`）。
- 建议：分指数统计并列出缺失最多的指数；预热期改为"资产自身预热 + 首次有效候选池规模"
  口径，单只后上市指数不应把整段回测标成预热。

### F-7 [P0] 行情表存在非交易日数据，回测照单全收

- 现象：2018-06-18（端午节假期，本地 `trading_calendar` 已正确标为非交易日）
  仍有 7 条日线（`930601/930713/H11059/H30022/H30199/H30217/H30269`）；
  回测把它当交易日，产生 1 天假调仓日、1 天假收益，并连带 6 条
  `DATA_GAP`/`BENCHMARK_MISSING` 警告和一个 `size=0` 的候选池片段。
- 核查范围：2016-2025 全区间**仅此 1 天 7 条**，属可清理的小规模污染
  （来源应是上游把上一交易日收盘价打在了假期日期上）。
- 建议：摄取时按 `trading_calendar` 过滤；回测侧对"有行情但不是交易日"显式告警并忽略。

### F-8 [P2] 候选池被 `MISSING_OPEN` 静默剔除，配置 21 只指数实际只有 10~14 只可交易

- 现象：基线回测的 `stability.candidate_pool` 显示 median 14/21、覆盖率 72.3%；
  `exclusions` 显示 `931994` 到 2024-10-22、`H30199` 到 2021-12-09、
  `H11059` 到 2023-03-15 都因 `MISSING_OPEN` 被剔除。
- 数据侧（2016-2025，`open_price` 为空的天数/总天数）：
  `931994` 2138/2430、`H11059` 1645/2431、`H30199` 1447/2431、`931719` 1316/2430、
  `930601` 1127/2431、`931743` 1099/1700、`931152` 909/2430、`H30269` 466/2431；
  同期 `close_price` 缺失为 **0**。
- 影响：策略实际资产域远小于配置，且"缺开盘价"更可能是数据源字段缺失而非市场事实
  （同一天有收盘价）；这类剔除会系统性改变回测语义，而摘要只给 info 级提示。
- 建议：执行模型降级（缺开盘价回退当日收盘/次日收盘）或标记 `UNTRADABLE`
  并在回测摘要里单列"因数据缺失被剔除的资产/天数"。

### F-9 [P2] `research batch` 缺变体排名摘要，实际吞吐是分钟级不是秒级

- 现象：23 个变体 × 5 窗口的完整 JSON 输出 4.7 万 token，超过工具输出上限被截断后
  只能重跑；`--no-json` 只是 `print(dict)`，没有表格视图。本轮临时用
  `docs/research/主线RS轮动v8/round2/summarize.py` 补了这个视图。
- 吞吐：窗口缓存（D-1）只消除了行情与因子的重算，逐日引擎循环仍是
  O(变体 × 交易日) ≈ **90 秒/变体**（5 个两年窗口、与其它任务并发时），
  23 个变体约 30 分钟；"秒级"只在小批量（3 变体 × 1 年窗口，74 秒）成立。
- 建议：`research batch --summary` 输出"变体 × 指标"排名表 + Δ vs 基线 + 劣化窗口占比；
  skill 里的"秒级"表述改为"分钟级，靠多进程而非缓存扩容"。

### F-10 [P2] 分段拼接的 aggregate 与整段回测口径不等价

- 现象：同一配置，整段 2016-2025 回测毛累计 86.67% / 夏普 0.4869；
  `research batch` 把 5 个两年窗口拼起来是 73.52% / 0.4400
  （窗口起点重置仓位、每窗重跑预热，窗口首尾有重复/缺失交易）。
- 建议：`caliber` 里标注 `aggregate_mode: window_stitched`，
  并在报告模板里禁止拿拼接数字与 `backtest show` 的整段数字直接比较。

### F-11 [P2] `strategy show` 顶层 `index_codes` 为空

- 现象：`strategy show mainline_rs_rotation_v8` 返回 `index_codes: []`，
  实际资产池在 `config_json.index_codes`（21 个）；而 `GET /strategies` 列表能返回 21 个。
- 影响：任何按详情顶层字段判断"这个策略有没有资产池"的调用都会误判
  （写脚本/写 skill 时很容易踩）。
- 建议：详情接口的 `index_codes` 从 `config_json` 回填，或删除这个冗余字段。

### F-12 [P2] `backtest list` 的净口径字段恒为 null

- 现象：`backtest list` 返回的 `metrics.net_sharpe_ratio / net_annualized_return_pct /
  annualized_turnover` 全是 null（只有 `backtest show` 即时算的才有）。
- 影响：列表无法按净口径排序/筛选，"换手 15 倍、成本吃掉全部超额"这类结论要逐条 show。
- 建议：列表路径也带上成本四档摘要（读取路径现算成本极低），或至少带 10bp 净指标。

### F-13 [P1] 稳健性批次的吞吐被"回测 lane 默认 1 worker"限制

- 现象：`--preset quick`（2 窗口 × 8 旋钮 = 34 条 5 年回测）在同步串行下预计需
  **约 100 分钟**（实测 5 年窗口 2~4.5 分钟/条）；要并行只能人工起多个
  `--sync` 进程，且批次之间没有并发度参数。
- 建议：给 `robustness scan/ablate/pool` 增加本地并行度（进程池跑 `--sync` 变体），
  或在文档里明确"并行 = 多起几个 CLI 进程 + 注意远端数据库连接预算"。

### F-14 [P1] `robustness stats` 在窗口数很少时给出无信息量的 PBO=0

- 现象：`--preset quick` 的批次只有 2 个窗口，`stats` 仍输出
  `pbo: {value: 0.0, n_splits: 2, n_blocks: 2}`。CSCV 的 PBO 在 2 个 split 下
  恒为 0（只有一种"训练/测试"切分），但这个 0.0 会被读成"过拟合概率为零"。
- 建议：`n_splits` 低于阈值（如 8）时 `pbo.value` 返回 null 并给出
  `reason="窗口数不足"`；或让 `stats` 支持按更细的时间块（而非整段窗口）切分。

### F-15 [P2] `optimization show` 不返回验收清单

- 现象：7 项验收清单只在 `optimization finish`（会改状态的命令）内部计算，
  `optimization show` 看不到逐项结论；agent 想在收尾前知道"哪一项没过、差多少"，
  只能先跑 `finish`（不可逆）或自己重算。
- 建议：`optimization show`（或 `--checklist`）直接返回 `_acceptance_checklist()`。

### F-16 [P2] 优化会话输出里净口径字段恒为 null

- 现象：`optimization evaluate` / `show` 的 `metrics_full`、`metrics_folds` 中
  `net_sharpe_ratio`、`net_annualized_return_pct`、`annualized_turnover` 全为 null；
  服务内部（`_check_net_cost`）确实现算了逐折净夏普，但结果只体现在验收清单里，
  报告要写净口径数字时只能另跑脚本（本轮用 `round2/fold_net.py` 补算）。
- 建议：把逐折/全区间的净夏普、净年化、年化换手一并写进会话指标字典。

### F-17 [P1] `optimization finish --promote` 不更新策略描述，版本说明与配置漂移

- 现象：v8 的版本历史（v1.2~v1.6 每次改了什么、结果如何）全部写在
  `strategy_config.description` 里；`--promote` 只回写 `config_json` 与 `version`，
  promote 之后描述仍写着"过滤条件 close_price 高于 ma_10d **与 ma_17d**"，
  与实际配置（已删 ma_17d）矛盾。
- 影响：描述是人和 agent 读策略的第一入口，漂移会让后续分析基于错误前提
  （尤其"下一轮该改什么"的判断）。
- 建议：promote 时把候选版本号 + 假设 + 关键指标追加进 description（或改成结构化
  `version_history` 字段），至少要在 `finish` 的输出里明确提示"描述未更新"。

### F-18 [P1] `optimization start --start` 缺省只覆盖最近 2 年

- 现象：`optimization start` 的 `start` 缺省是 `end - 730 天`，`end` 缺省才是研究期末端。
  忘了写 `--start 2016-01-01` 时，会话会**静默**只评估最近两年，
  而验收清单、`fold_summary` 仍然照常输出"通过"，看不出覆盖范围被缩小了 5 倍。
- 影响：与"优化必须在完整研究期上评估"的约定直接冲突，且是静默的。
- 建议：缺省改为 `research_period_start`；或在 `--start` 缺省时把
  "本次仅评估 X 年"写进会话与报告显著位置。

---

## G. 第三轮平台修复（2026-09-14，已修复）

> 本轮来自一次对"回测流程 / 过拟合判断 / 策略周期管理"的逐参数审计（含代码路径推演
> 与临时脚本验证）。**口径变更**：受影响路径（`t_plus_1_close`、周/月度调仓、
> point-in-time 标的池、市场级因子集合）重跑后的数值与历史回测不可直接比较，
> 验收回测需按新口径重跑。

### G-1 [P0] `t_plus_1_close` 收益归属滞后一天（实际等价 T+2 收盘执行）

- 现象：成交发生在 `close_{T+1}`，但该行收益用 `executed_positions = prev_positions`
  （更旧的仓位）计算，新仓位的首个收益区间被旧仓位吃掉；同一行的
  `total_exposure/cash_ratio` 也按旧仓位记录，而 `turnover` 按新目标计。
- 修复：`execution_model == "t_plus_1_close"` 分支改为
  `executed_positions = pending_positions or prev_positions`，并据此计算收益、
  落库 `positions`、暴露率与 `next_positions`（`services/backtest_service.py` 主循环）。
- 测试：`tests/unit/test_backtest_execution_model.py::TestCloseExecutionAttribution`
  （含 T+1 开盘执行的对照组）。

### G-2 [P0] 周度调仓在目标日休市时整周不调仓；月度存在同类跨月失效

- 现象：`_is_nearest_in_window` 对 `days_since_target < 0` 直接返回 False，因此
  "目标周五休市 → 顺延到下周一"的路径不可达（实测周四 False / 下周一 False）；
  月度在目标日休市延续到下月首个交易日时同样整月跳过；另有
  `current_date.weekday() == target` 会在非交易日返回 True 的隐患。
- 修复：`domain/strategies/rebalance.py` 改为统一的"目标日（含）之后的第一个交易日"
  判定（`_is_carried_target`：从目标日逐日推进，中途出现交易日即不调仓），
  目标日取当前或上一个自然周/月的目标日；月度目标日超出当月天数时 clamp 到月末；
  非交易日一律返回 False。删除固定 10 天窗口与死分支。
- 测试：`tests/unit/test_rebalance_carry_forward.py`（同周顺延、跨周顺延、跨月顺延、
  月末 clamp、非交易日、日历异常上抛）。

### G-3 [P1] 调仓配置缺少枚举/范围校验，拼写错误静默退化为每日调仓

- 修复：`RebalanceConfig.frequency` 收紧为 `Literal[daily|weekly|monthly]`，
  `day_of_week ∈ [0,4]`、`day_of_month ∈ [1,31]`；`day_of_month > 28` 在
  `StrategyConfigService._structural_validation` 给出 warning（月末语义随月份变化）。
- 测试：`test_rebalance_carry_forward.py::TestRebalanceConfigValidation`。

### G-4 [P1] 回测标的池是"活到今天的资产"（幸存者偏差），注释与逻辑相反

- 现象：`_resolve_index_universe` 只取 `is_active=True` 并显式剔除退市指数，
  docstring 却写"避免幸存者偏差"；`IndexService.remove_index` 也不记录退市日，
  使 point-in-time 判定无从下手。
- 修复：新增 `BenchmarkIndexRepository.find_for_period(start_date)`
  （`is_active OR delisting_date IS NULL OR delisting_date >= start_date`）；
  `_resolve_index_universe` 改用区间起点并给 universe 项补 `is_active` 标记；
  `remove_index` 记录退市日（API 新增可选 `delisting_date`），重新激活时清空；
  回测产出 `UNIVERSE_INCLUDES_DELISTED` info 提示；
  `_ensure_market_scope_bars` 同步改用 point-in-time 集合。
- 测试：`tests/unit/test_backtest_universe_pit.py`、`test_market_scope_factor.py`。

### G-5 [P1] 验收清单"参数邻域"可复用过期证据

- 现象：`_check_neighborhood` 只按 `strategy_id` 取最近成功的 scan 批次，
  promote 之后基线配置已变，上一轮的批次仍让本项通过。
- 修复：证据必须匹配当前配置（`baseline_config_hash` = 会话基线或候选哈希，
  或批次直接跑在候选策略上），并把批次 ID / preset / windows / 容差写入
  `evidence`；`finish` 的验收清单**默认强制**（`--no-strict` 才跳过）。
- 测试：`tests/unit/test_optimization_neighborhood_evidence.py`、
  `test_optimization_service.py::test_accept_requires_checklist_by_default`。

### G-6 [P1] Deflated Sharpe 的试验次数台账漏计优化会话

- 现象：`_trial_ledger` 只累加稳健性批次 `trial_count`，优化会话未计入，
  使 N 偏小、DSR 偏乐观（文档却声称两者都是来源）。
- 修复：台账 = 稳健性变体数 + 已评估优化会话数，并在 `statistics.n_trials_breakdown`
  给出拆分。
- 测试：`tests/unit/test_f_robustness_fixes.py::TestTrialLedger`。

### G-7 [P2] 逐折聚合未配对，Δ 与胜出折数建立在不同样本上

- 修复：`_compute_fold_summary` 只统计基线/候选两侧都有值的**配对折**，
  新增 `paired_folds` / `evaluated_folds`（与稳健性"共同窗口"原则一致）。
- 测试：`test_optimization_service.py::TestFoldSummary`。

### G-8 [P2] 统计量口径的可审计性

- 修复：PBO 增加 `power_note`（对称切分 < 10 时提示分辨率低）；
  邻域容差改为可配置（`QUANT_ETF_ROBUSTNESS_NEIGHBORHOOD_TOLERANCE`，默认 0.1）
  并随结果落库；块自助法改为循环块（尾部不再被系统性欠采样）。
- 测试：`test_research_robustness.py::TestBootstrap`。

### G-9 [P2] 指标口径易混淆与"数据不足"被记成健康

- 修复：新增 `annualized_excess_return_pct`（毛口径年化超额）与
  `benchmark_up_days_pct`（基准上涨日占比，作为 `signal_accuracy_pct` 的参照）；
  `BacktestStability.warmup_trading_days` 暴露预热期；`signal_accuracy_pct`
  在前端改名为"持仓次日正收益率"并说明其含市场 β；
  `assess_health` 在样本不足时返回 `UNKNOWN` 而不是 `HEALTHY`；
  候选池剔除原因区分当日（`MISSING_CLOSE`/`MISSING_HIGH_LOW`）与次日
  （`MISSING_NEXT_BAR`/`MISSING_NEXT_HIGH_LOW`/`MISSING_OPEN`）。

### G-10 已知设计取舍（本轮不改，仅记录）

- 候选池条件 4 需要"次日行情是否存在"，属 ex-ante 可执行域约束；若不剔除，
  缺次日行情的资产会以 0 收益参与组合，反而系统性低估波动（已在 docstring 说明）。
- 最后交易日收益恒记 0（策略与基准同口径对称，见 `domain/portfolio/benchmark.py`）。
- 统计模块用总体标准差、绩效模块用样本标准差（与 v8 验证脚本对齐）。
- **优化折不是样本外**：候选是看过全区间结果后提出的，4 折只是同一研究期内的一致性
  检查；真正的样本外只有 2026-01-01 起的验证期，且一旦观察即被 `validation_consumed_at` 消费。
